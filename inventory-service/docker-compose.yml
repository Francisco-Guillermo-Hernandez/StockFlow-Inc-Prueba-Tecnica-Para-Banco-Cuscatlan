services:
  inventory-service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3030:3030"
    environment:
      - SERVER_PORT=3030
      - SPRING_PROFILES_ACTIVE=default
    restart: always
